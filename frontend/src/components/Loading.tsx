import { FC } from "react";

export const Loading:FC = () => {
    return (
        <span className="flex items-center justify-center  h-[100vh] pb-[70vh] w-full text-amber-200 opacity-50 text-8xl">
            Loading...
        </span>
    )
}